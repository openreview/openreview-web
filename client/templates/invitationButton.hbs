<div class="panel">
  {{#if options.largeLabel}}
    <strong class="item hint">New:</strong>
  {{else}}
    <span class="item hint">New:</span>
  {{/if}}
  <button class="btn">{{prettyId invitationId}}</button>
</div>
