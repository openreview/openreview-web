<div id="message-group-members-modal" class="modal fade" tabindex="-1" role="dialog" >
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
        <h3 class="modal-title">Message Group Members</h3>
      </div>

      <div class="modal-body step-1">
        <div class="alert alert-danger" style="display: none;">
          <span>An error occurred</span>
        </div>

        <p>Enter a subject and message below. Your message will be sent via email to the following {{numRecipients}} group member(s):</p>
        <div class="well reviewer-list">
          {{join recipientNames}}
        </div>

        <form id="message-group-members-form" method="POST">
          <div class="form-group">
            <label for="subject">Email Subject</label>
            <input type="text" name="subject" class="form-control" placeholder="Subject" value="{{defaultSubject}}" required>
          </div>

          <div class="form-group">
            <label for="message">Email Body</label>
            <p class="hint">Hint: You can personalize emails using template variables. The text \{{firstname}} and \{{fullname}} will automatically be replaced with the recipient's first or full name if they have an OpenReview profile. If a profile isn't found their email address will be used instead.</p>
            <textarea name="message" class="form-control feedback-input" rows="6" placeholder="Message" required>{{defaultBody}}</textarea>
          </div>
        </form>
      </div>

      <div class="modal-footer">
        <button type="button" class="btn btn-default" data-dismiss="modal">Cancel</button>
        <button type="button" class="btn btn-primary">Send Messages</button>
      </div>
    </div>
  </div>
</div>
