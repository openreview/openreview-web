import CommonLayout from '../../CommonLayout'

export default function Layout({ children }) {
  return <CommonLayout>{children}</CommonLayout>
}

export const metadata = {
  title: 'Terms of Use | OpenReview',
}
