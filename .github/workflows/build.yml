# This workflow will do a clean install, build the source code and run tests across different versions of node
# See: https://help.github.com/actions/language-and-framework-guides/using-nodejs-with-github-actions

name: Next.js CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:
    env:
      API_URL: http://localhost:3000
      NEXT_PORT: 3030
      SUPER_USER: openreview.net

    runs-on: ubuntu-latest

    services:
      mongodb:
        image: mongo:4.2
        ports:
          - 27017:27017
      redis:
        image: redis
        ports:
          - 6379:6379

    strategy:
      matrix:
        node-version: [14.x]

    steps:
    - uses: actions/checkout@v2
    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v1
      with:
        node-version: ${{ matrix.node-version }}
    - name: Configure sysctl Limits
      run: |
        sudo swapoff -a
        sudo sysctl -w vm.swappiness=1
        sudo sysctl -w fs.file-max=262144
        sudo sysctl -w vm.max_map_count=262144
    - name: Run Elasticsearch
      uses: elastic/elastic-github-actions/elasticsearch@master
      with:
        stack-version: 7.6.0
    - name: Checkout OpenReview API
      uses: actions/checkout@v2
      with:
        repository: openreview/openreview
        token: ${{ secrets.OPENREVIEW_ACCESS_TOKEN }}
        path: openreview-api
    - name: Run tree
      run: |
        echo 6
      #  apt-get install tree
      #  tree
    - name: Cache OpenReview API
      uses: actions/cache@v2
      with:
        path: |
          ~/.npm
        key: ${{ runner.os }}-node-${{ hashFiles('openreview-api/package-lock.json') }}
    - name: Cache OpenReview API Node Modules
      id: cache-openreview-api
      uses: actions/cache@v2
      with:
        path: |
          openreview-api/node_modules
        key: ${{ runner.os }}-node-modules-${{ hashFiles('openreview-api/package-lock.json') }}
    - name: Run OpenReview API 1
      run: |
        cd openreview-api
        mkdir logs
        mkdir files
        mkdir files/attachments
        mkdir files/pdfs
    - name: Run OpenReview API 2
      if: steps.cache-openreview-api.outputs.cache-hit != 'true'
      run: |
        cd openreview-api
        npm install
    - name: Run OpenReview API 3
      run: |
        cd openreview-api
        NODE_ENV=circleci node scripts/clean_start_app.js &
    - name: Cache OpenReview Web
      id: cache-openreview-web
      uses: actions/cache@v2
      with:
        path: |
          node_modules
        key: ${{ runner.os }}--web-node-modules-${{ hashFiles('package-lock.json') }}
    - name: Install Package Web
      if: steps.cache-openreview-web.outputs.cache-hit != 'true'
      run: npm install
    - run: npm run lint
    - run: npm run build
    - run: npm run start &
    - name: Run TestCafe Tests
      uses: DevExpress/testcafe-action@latest
    - name: Upload Test Videos
      if: ${{ failure() }}
      uses: actions/upload-artifact@v2
      with:
        name: videos
        path: /home/runner/work/openreview-web/openreview-web/videos/
    - name: Check API Logs
      if: ${{ failure() }}
      run: |
        cd openreview-api/logs
        cat "$(ls -rt | tail -n1)"
